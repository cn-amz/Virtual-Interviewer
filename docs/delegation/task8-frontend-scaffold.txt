Summary:
Created the Vite React TypeScript frontend scaffold with 7 files. The build passed — TypeScript compilation (`tsc`) and Vite production build both succeeded. The `package-lock.json` was generated by npm install and left for Codex review.

Files changed:
- `apps/web/package.json` — project metadata, scripts (dev/build/test), dependencies (react 18, vite, typescript), devDependencies (vitest, @types/react, @types/react-dom)
- `apps/web/index.html` — HTML shell with zh-CN lang, root mount point, module script entry
- `apps/web/vite.config.ts` — Vite config with React plugin, port 5173
- `apps/web/tsconfig.json` — strict TypeScript config targeting ES2020 with react-jsx
- `apps/web/src/main.tsx` — React 18 createRoot entry point with StrictMode
- `apps/web/src/App.tsx` — simple app shell with Chinese title/copy for realtime virtual interviewer
- `apps/web/src/styles.css` — minimal responsive styles with Inter/Microsoft YaHei font stack
- `apps/web/package-lock.json` — generated by npm install (not committed)

Verification:
- `npm install` — 94 packages installed successfully
- `npm run build` — `tsc` type-check passed, Vite built 3 output files (HTML, CSS, JS) in 538ms

Notes/Risks:
- Added `@types/react` and `@types/react-dom` to devDependencies (not in the plan's package.json). These are required for TypeScript to compile JSX — without them, `tsc` fails with "JSX element implicitly has type 'any'" errors.
